---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const chapters = (await getCollection("chapters", ({ data }) => data.status === "active"))
  .sort((a, b) => a.data.order - b.data.order);
const latest = chapters.at(-1);
---

<BaseLayout
  title="全中国最激烈故事 · 在线阅读"
  description="《全中国最激烈故事》在线阅读站，提供章节阅读、目录浏览与离线下载。"
>
  <section class="manifesto">
    <h2 class="title-main">全中国最<br/>激烈故事</h2>
    <blockquote class="quote">
      "我是玉皇大帝的女婿！丈人叫我领十万天兵……
       阎罗大王做先锋，五道将军做合后，
       给一颗金印，重八百余斤，来杀你们这帮鸟人！"
    </blockquote>
  </section>

  <img
    class="cover"
    src="/images/COVER.jpg"
    alt="封面"
    width="1184"
    height="823"
    decoding="async"
  />

  <section class="entry">
    <div class="actions">
      <a class="primary" href="/book/book-1/chapter/b1-c001">开始阅读</a>
      <a id="continue-reading" href="#" style="display:none">继续阅读</a>
      {latest ? <a href={`/book/${latest.data.book_id}/chapter/${latest.data.chapter_id}`}>最新章节</a> : null}
      <a href="/book/book-1">章节目录</a>
    </div>
    <div class="download">
      <p class="download-label">📥 下载离线版</p>
      <div class="actions">
        <a href="/downloads/全中国最激烈故事第一部—泰山府.pdf" download target="_blank">下载 PDF</a>
        <a href="https://www.ilanzou.com/s/jCnnPSlv" target="_blank">蓝奏云下载</a>
      </div>
    </div>
    <div class="meta-info">
      <p>前《无标题艺术》</p>
      <p>知乎专栏：<a href="https://www.zhihu.com/column/c_1838755902276120576" target="_blank">全中国最激烈故事</a></p>
      <p>by <a href="https://www.zhihu.com/people/ru-quan-cun-kang-ba-zi-86" target="_blank">泰山府在此</a></p>
    </div>
    <div class="meta-info">
      <p class="meta-title">友情链接</p>
      <p><a href="https://ljt-official.github.io/ljt/" target="_blank">《垃圾桶选集》</a></p>
    </div>
  </section>

  <script is:inline>
    (function(){
      function readHistory() {
        try {
          var raw = localStorage.getItem('readHistory');
          var parsed = JSON.parse(raw || '[]');
          return Array.isArray(parsed) ? parsed : [];
        } catch (_) {
          return [];
        }
      }

      var hist = readHistory();
      if(!hist.length) return;
      var last = hist[0];
      var el = document.getElementById('continue-reading');
      if (!(el instanceof HTMLAnchorElement)) return;
      el.href = '/book/'+last.book+'/chapter/'+last.id;
      el.textContent = '继续阅读：'+last.title;
      el.style.display = '';
    })();
  </script>
</BaseLayout>

<style>
  .manifesto {
    padding: 2rem 0 1.5rem 1.5rem;
    position: relative;
  }
  .manifesto::after {
    content: "";
    display: block;
    width: 60px;
    height: 3px;
    background: var(--accent);
    margin-top: 1.5rem;
  }
  .title-main {
    font-family: "Noto Sans SC", sans-serif;
    font-weight: 900;
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    line-height: 1.1;
    margin: 0 0 1.2rem;
    letter-spacing: -0.02em;
  }
  :global(html.dark) .title-main {
    text-shadow: 0 0 40px var(--accent-glow);
  }
  .quote {
    margin: 0 0 1rem;
    padding-left: 1rem;
    border-left: 3px solid var(--accent);
    color: var(--accent);
    font-weight: 700;
    line-height: 1.8;
    font-size: 0.95rem;
  }
  :global(html.dark) .quote {
    text-shadow: 0 0 20px var(--accent-glow);
  }
  .cover {
    display: block;
    max-width: 100%;
    width: 100%;
    margin: 1.5rem 0 0 1.5rem;
    border: 1px solid var(--line);
  }
  .entry {
    padding-left: 1.5rem;
  }
  .actions {
    margin-top: 1.2rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  .actions a {
    text-decoration: none;
    padding: 0.45rem 0.9rem;
    border-radius: 0;
    border: 1px solid var(--line);
    color: var(--bg-ink);
    font-size: 0.92rem;
    transition: border-color .2s;
  }
  .actions a:hover { border-color: var(--accent); color: var(--accent); }
  .actions a.primary {
    background: var(--accent);
    color: #fff;
    border-color: var(--accent);
  }
  .actions a.primary:hover { opacity: 0.85; color: #fff; }
  .download {
    margin-top: 1.5rem;
    padding-top: 1.2rem;
    border-top: 1px solid var(--line);
  }
  .download-label {
    margin: 0 0 0.6rem;
    font-size: 0.92rem;
    color: var(--muted);
  }
  .meta-info {
    margin-top: 1.2rem;
    padding-top: 1rem;
    border-top: 1px solid var(--line);
    font-size: 0.88rem;
    color: var(--muted);
    line-height: 1.8;
  }
  .meta-info p { margin: 0; }
  .meta-info a {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 2px;
  }
  .meta-info a:hover { opacity: 0.75; }
  .meta-title {
    font-weight: 700;
    color: var(--fg);
    margin-bottom: 0.2rem;
  }
  @media (max-width: 640px) {
    .manifesto, .entry { padding-left: 0.5rem; }
    .cover { margin-left: 0.5rem; }
  }
</style>
